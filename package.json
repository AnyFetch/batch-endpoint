{
  "name": "batch-endpoint",
  "version": "0.0.2",
  "description": "A restify plugin for batch-querying `GET` endpoints.",
  "author": "Matthieu Bacconnier <matthieu.bacconnier@papiel.fr>",
  "scripts": {
    "test": "NODE_ENV=test mocha -R spec test/",
    "coverage": "NODE_ENV=test istanbul cover node_modules/mocha/bin/_mocha -- -R spec test/"
  },
  "main": "./lib",
  "repository": {
    "type": "git",
    "url": "https://github.com/AnyFetch/batch-endpoint/"
  },
  "keywords": [
    "batch",
    "bulk",
    "restify"
  ],
  "dependencies": {
    "async": "0.9.x",
    "supertest": "~0.15.0",
    "restify": "2.8.x"
  },
  "devDependencies": {
    "mocha": "2.0.x",
    "should": "4.3.x"
  },
  "licence": "WTFPL",
  "engines": {
    "node": ">=0.10"
  }
}
